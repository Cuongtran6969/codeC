#include"stdio.h"
#include"string.h"
int main() {
	int i, j;
	char str[100];
	for(i=0; i<7; i++) {
		scanf("%c", &str[i]);
		fflush(stdin);
	}
	int var, count, address;
	for(i=0; i<7; i++) {
	  	
	}
}
